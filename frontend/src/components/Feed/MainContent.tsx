import React from "react";
import { Box } from "@primer/react";

type Props = {};

const MainContent = (props: Props) => {
  return (
    <Box p={3} color="fg.onEmphasis" bg="canvas.primary" border="1px solid red">
      2
    </Box>
  );
};

export default MainContent;
